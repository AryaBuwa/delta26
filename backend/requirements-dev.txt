# requirements-dev.txt
# Local development only — never deployed to Render
# Install with: pip install -r requirements-dev.txt

# Simulation + historical data (simulation.py only)
statsbombpy==1.19.0

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0

# MLOps (local tracking + model versioning)
mlflow==2.17.2
dvc==3.56.0
dvc[gdrive]

# Code quality
black==24.10.0